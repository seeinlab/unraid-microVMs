Menu="Tasks:65"
Type="xmenu"
Code="e918"
Lock="true"
Tabs="true"
Cond="is_file('/usr/local/bin/cloud-hypervisor') || is_file('/usr/local/bin/firecracker')"
---
<?php
$plugin = "microvms";
$docroot = $docroot ?? $_SERVER['DOCUMENT_ROOT'] ?: '/usr/local/emhttp';
require_once "$docroot/plugins/$plugin/include/common.php";

$cfg = microvm_load_config();
$noticeMessage = null;

if (($var['fsState'] ?? '') != "Started") {
    $noticeMessage = "Array must be <b>started</b> to manage microVMs";
} elseif (($cfg['SERVICE'] ?? 'disable') !== 'enable') {
    $noticeMessage = "microVMs service is disabled. <a href='/Settings/MicroVMsControlplane'>Enable in Settings</a>";
}
?>
<?php if ($noticeMessage): ?>
<p class="notice"><?=$noticeMessage?></p>
<?php endif; ?>
